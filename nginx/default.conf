server {
    listen 80;

    location / {
        proxy_pass http://backend:9000;  # Matches the service name in docker-compose
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
