version: "3.9"

services:
  backend:
    build: .
    container_name: mobilecomm-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./downloads:/app/downloads
